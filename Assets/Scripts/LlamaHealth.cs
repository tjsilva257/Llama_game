using UnityEngine;

public class LlamaHealth : MonoBehaviour
{
    // Variable: fall threshold (how low before eliminated?)
    // Variable: is this the player?
    
    void Update()
    {
        // Check if Y position is below threshold
        // If yes:
        //   - If player: print "Game Over" (for now)
        //   - If NPC: destroy this gameobject
    }
}